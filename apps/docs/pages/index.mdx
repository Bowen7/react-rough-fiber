import { Tab, Tabs, Callout } from 'nextra-theme-docs'
import { RoughSVG } from 'react-rough-fiber'
import { IconDemo } from "components/demos/icon"
import { ChartDemo } from "components/demos/chart"
import { TextDemo } from "components/demos/text"
import { EmojiDemo } from "components/demos/emoji"
import { Banner } from "components/banner"

# Get Started
 
## Introduction

`react-rough-fiber` is a React renderer for rendering hand-drawn SVGs.

<Banner />

## Installation

<Tabs items={['pnpm', 'npm', 'yarn']}>
  <Tab>
    ```bash copy
    pnpm i react-rough-fiber react-reconciler
    ```
  </Tab>
  <Tab>
    ```bash copy
    npm i react-rough-fiber react-reconciler
    ```
  </Tab>
  <Tab>
    ```bash copy
    yarn add react-rough-fiber react-reconciler
    ```
  </Tab>
</Tabs>

<Callout type="warning" emoji="⚠️">
  If you are using React 17, you need to install `react-reconciler@0.26.2`.
</Callout>

## Quick Start

To render the SVG in a hand-drawn style, you only need to add **three lines** of code:

```jsx {1,4,8}
import { RoughSVG } from 'react-rough-fiber'
export default function App() {
  return (
    <RoughSVG>
      <svg viewBox="0 0 128 128" width="128" height="128">
        <circle cx={64} cy={64} r={48} stroke="currentColor" fill="#82ca9d" />
      </svg>
    </RoughSVG>
  )
}
```

<RoughSVG className="flex justify-center">
  <svg viewBox="0 0 128 128" width="128" height="128">
    <circle
      cx={64}
      cy={64}
      r={48}
      stroke="currentColor"
      fill="#82ca9d"
    />
  </svg>
</RoughSVG>

## Usage

react-rough-fiber can integrate with many existing SVG-based libraries:

### Icons

<IconDemo />

[Code](/examples/icon)

### Emoji

<EmojiDemo />

[Code](/examples/emoji)

### Chart

<ChartDemo />

[Code](/examples/chart/recharts)

### Text

<TextDemo />

[Code](/examples/text)
