import { JSXSandbox } from "components/sandboxes/jsx"

# JSX

Convert JSX to SVG using [satori](https://github.com/vercel/satori)(it supports a subset of the spec that covers most common CSS features).

<JSXSandbox />